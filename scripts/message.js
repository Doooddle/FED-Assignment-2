/*jshint esversion: 6 */ 

// This function is used to show a pop up message after submitting.

function showMessage(message) {
    var confirmation = confirm(message);   //message shown is specific to the form (contact, login, register)
}